/** @format */

const array = [1, 21, 13, 88, 19];
const object = {
  name: "Prashant",
};

console.log("Array is ->", Array.isArray(array));
console.log("Object is ->", Array.isArray(object));

/**
 * Output: node 2.Check_isArray_or_Object.js 
    Array is -> true
    Object is -> false
 */
